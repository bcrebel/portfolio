---
interface Props {
	label?: string;
	title: string;
}
const { label, title }: Props = Astro.props;
---

<div class="section-header">
	{label && <div class="section-label">{label}</div>}
	<h2>{title}</h2>
</div>


